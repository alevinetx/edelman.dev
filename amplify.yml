version: 1
env:
  variables:
    NEXT_PUBLIC_SANITY_DATASET: production
    NEXT_PUBLIC_SANITY_PROJECT_ID: 1os98t11
    NODE_ENV: production
frontend:
  phases:
    preBuild:
      commands:
        - yarn
        - cd studio
        - yarn
        - cd ..
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*