version: 1
env:
  variables:
    NEXT_PUBLIC_SANITY_DATASET: production
    NEXT_PUBLIC_SANITY_PROJECT_ID: 1os98t11
    NODE_ENV: production
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g lerna
        - npm install -g @sanity/cli
        - yarn install
        - cd studio
        - yarn install
        - cd ..
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .yarn/**/*
      - .next/**/*